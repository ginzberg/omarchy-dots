# Application bindings
$terminal = uwsm-app -- $TERMINAL
$browser = omarchy-launch-browser

bindd = SUPER SHIFT, RETURN, Terminal, exec, $terminal --working-directory="$(omarchy-cmd-terminal-cwd)"
bindd = SUPER SHIFT, F, File manager, exec, uwsm-app -- nautilus --new-window
bindd = SUPER SHIFT, B, Browser, exec, $browser
bindd = SUPER SHIFT ALT, B, Browser (private), exec, $browser --private
bindd = SUPER SHIFT, M, Music, exec, omarchy-launch-or-focus spotify
bindd = SUPER SHIFT, N, Editor, exec, omarchy-launch-editor
bindd = SUPER SHIFT, T, Activity, exec, $terminal -e btop
bindd = SUPER SHIFT, D, Docker, exec, $terminal -e lazydocker
bindd = SUPER SHIFT, G, Signal, exec, omarchy-launch-or-focus signal "uwsm-app -- signal-desktop"
bindd = SUPER SHIFT, O, Obsidian, exec, omarchy-launch-or-focus "^obsidian$" "uwsm-app -- obsidian -disable-gpu --enable-wayland-ime"
bindd = SUPER SHIFT, W, Typora, exec, uwsm-app -- typora
bindd = SUPER SHIFT, SLASH, Passwords, exec, uwsm-app -- 1password

# If your web app url contains #, type it as ## to prevent hyprland treating it as a comment
bindd = SUPER SHIFT, A, ChatGPT, exec, omarchy-launch-webapp "https://chatgpt.com"
bindd = SUPER SHIFT ALT, A, Grok, exec, omarchy-launch-webapp "https://grok.com"
bindd = SUPER SHIFT, C, Calendar, exec, omarchy-launch-webapp "https://app.hey.com/calendar/weeks/"
bindd = SUPER SHIFT, E, Email, exec, omarchy-launch-webapp "https://app.hey.com"
bindd = SUPER SHIFT, Y, YouTube, exec, omarchy-launch-webapp "https://youtube.com/"
bindd = SUPER SHIFT ALT, G, WhatsApp, exec, omarchy-launch-or-focus-webapp WhatsApp "https://web.whatsapp.com/"
bindd = SUPER SHIFT CTRL, G, Google Messages, exec, omarchy-launch-or-focus-webapp "Google Messages" "https://messages.google.com/web/conversations"
bindd = SUPER SHIFT, P, Google Photos, exec, omarchy-launch-or-focus-webapp "Google Photos" "https://photos.google.com/"
bindd = SUPER SHIFT, X, X, exec, omarchy-launch-webapp "https://x.com/"
bindd = SUPER SHIFT ALT, X, X Post, exec, omarchy-launch-webapp "https://x.com/compose/post"

# Overwrite existing bindings, like putting Omarchy Menu on Super + Space
# unbind = SUPER, SPACE
# bindd = SUPER, SPACE, Omarchy menu, exec, omarchy-menu
unbind = SUPER, K
unbind = SUPER SHIFT, RETURN
unbind = SUPER SHIFT, E
unbind = SUPER SHIFT, M
unbind = SUPER SHIFT, G
unbind = SUPER SHIFT ALT, A
unbind = SUPER SHIFT, C
unbind = SUPER SHIFT ALT, G
unbind = SUPER SHIFT CTRL, G
unbind = SUPER SHIFT, X
unbind = SUPER SHIFT ALT, X

$mainMod = SUPER

bindd = SUPER SHIFT, RETURN, Terminal, exec, $terminal --working-directory="$HOME"
bind = $mainMod SHIFT, C, killactive,
bind = $mainMod, F, togglefloating,
bind = $mainMod, M, fullscreen,
bind = $mainMod, J, layoutmsg, cyclenext
bind = $mainMod, K, layoutmsg, cycleprev
bind = $mainMod, return, layoutmsg, swapwithmaster auto #master layoutmsg
bindd = $mainMod, Z, Show key bindings, exec, omarchy-menu-keybindings

# Launch apps
bindd = $mainMod SHIFT, E, Email (Thunderbird), exec, thunderbird
bindd = $mainMod SHIFT, Q, unezQuake, exec, $HOME/.local/bin/unezquake-handler.sh

# monitor stuff
bind = $mainMod, comma, focusmonitor, 1 
bind = $mainMod, period, focusmonitor, 0 
bind = $mainMod SHIFT, comma, movewindow, mon:1
bind = $mainMod SHIFT, period, movewindow, mon:0

# Monitor mode switches
bindd = $mainMod CTRL, C, Couch mode (SDR), exec, $HOME/.local/bin/monitor-toggle.sh couch-mode
bindd = $mainMod CTRL, D, Desk mode (SDR), exec, $HOME/.local/bin/monitor-toggle.sh desk-mode
bindd = $mainMod CTRL, H, Hybrid mode (SDR), exec, $HOME/.local/bin/monitor-toggle.sh hybrid-mode
# HDR versions
bindd = $mainMod CTRL SHIFT, C, Couch mode (HDR), exec, $HOME/.local/bin/monitor-toggle.sh couch-mode hdr
bindd = $mainMod CTRL SHIFT, D, Desk mode (HDR), exec, $HOME/.local/bin/monitor-toggle.sh desk-mode hdr
bindd = $mainMod CTRL SHIFT, H, Hybrid mode (HDR), exec, $HOME/.local/bin/monitor-toggle.sh hybrid-mode hdr
